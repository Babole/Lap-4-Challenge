[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
flask = "*"
flask-cors = "*"

[dev-packages]
pytest = "*"

[requires]
python_version = "3.10"

[scripts]
start = "bash -c \"export FLASK_ENV='development' && flask run\""
dev = "bash -c \"export FLASK_ENV='development' && flask run --host=0.0.0.0\""
test = "pytest ."
coverage = "pytest --cov-report term-missing --cov=."
